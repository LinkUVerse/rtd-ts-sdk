# Copyright (c) Wenchuan Tech, Inc.
# SPDX-License-Identifier: Apache-2.0

query getCoins(
	$owner: RtdAddress!
	$first: Int
	$cursor: String
	$type: String! = "0x2::coin::Coin<0x2::rtd::RTD>"
) {
	address(address: $owner) {
		address
		objects(first: $first, after: $cursor, filter: { type: $type }) {
			pageInfo {
				hasNextPage
				endCursor
			}
			nodes {
				owner {
					...OBJECT_OWNER_FIELDS
				}
				contents {
					bcs
					json
					type {
						repr
					}
				}
				address
				version
				digest
				previousTransaction {
					digest
				}
			}
		}
	}
}
